# 1. System Configuration
APP_NAME="LMS Backend API"
APP_ENV=development
PORT=8000

# 2. Database Connection (PostgreSQL Async Connection)
# Cấu trúc: postgresql+asyncpg://<username>:<password>@<host>:<port>/<db_name>
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/lms_db

# 3. Security (JWT Secret Key - Nên chạy `openssl rand -hex 32` để gen chuỗi mới)
SECRET_KEY=YOUR_SUPER_SECRET_KEY_FOR_JWT_SIGNING_1234567890
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# 4. Momo Cổng thanh toán (Khi tích hợp thật)
MOMO_PARTNER_CODE=your_momo_partner_code
MOMO_ACCESS_KEY=your_momo_access_key
MOMO_SECRET_KEY=your_momo_secret_key
MOMO_ENDPOINT=https://test-payment.momo.vn/v2/gateway/api/create

# 5. VNPay Cổng thanh toán (Khi tích hợp thật)
VNPAY_TMN_CODE=your_vnpay_tmn_code
VNPAY_HASH_SECRET=your_vnpay_hash_secret
VNPAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
